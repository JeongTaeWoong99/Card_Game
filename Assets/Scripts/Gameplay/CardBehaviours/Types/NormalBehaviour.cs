using UnityEngine;

// 일반 — 특별한 후방 효과·대기 없음. 기본 근접 공격만 사용하므로 추가 로직이 없다.
// 데이터(이름·설명·대기턴)는 베이스의 직렬화 필드에서 인스펙터로 편집한다.
[CreateAssetMenu(fileName = "NormalBehaviour", menuName = "Scriptable Object/Card Behaviour/Normal")]
public class NormalBehaviour : CardBehaviour
{
}
